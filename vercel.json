{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": ".vuepress/dist"
      }
    }
  ],
  "buildCommand": "npm install && npm run docs:build"
}
